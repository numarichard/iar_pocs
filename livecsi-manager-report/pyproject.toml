[project]
name = "iar-poc-livecsi-table"
version = "0.1.0"
description = "POC: yaml-driven table view (LiveCSI manager report context)"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.0",
    "pyyaml>=6.0",
    "streamlit>=1.44",
]

[tool.uv]
package = false
