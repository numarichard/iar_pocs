[project]
name = "table-view-poc"
version = "0.1.0"
description = "POC: yaml-driven table view"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.0",
    "pyyaml>=6.0",
    "streamlit>=1.44",
]

[tool.uv]
package = false
